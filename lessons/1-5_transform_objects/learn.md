// Scene

// Cube
    // BoxGeometry
    // MeshBasicMaterial

// Camera
    // perspectiveCamera(fov, width/height, near, far)

// Render
    // Renderer
        // .setSize(width, height)
        // .render(scene, camera)